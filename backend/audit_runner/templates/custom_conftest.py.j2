import json
import pytest
from pathlib import Path

CONFIG_FILE = Path(__file__).parent.parent / "_config.txt"


@pytest.fixture(scope="session")
def device_config():
    """The device configuration text to audit."""
    return CONFIG_FILE.read_text()
